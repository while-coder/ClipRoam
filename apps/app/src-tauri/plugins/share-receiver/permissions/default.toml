"$schema" = "schemas/schema.json"

[default]
description = "Allows ClipRoam to receive Android share intents and notify the main window."
permissions = [
  "allow-register-listener",
  "allow-remove-listener",
]

# Changelog

本文件记录 ClipRoam App 与 Server 的发版说明。发版 workflow 会自动提取对应 `## <version>` 段落作为 GitHub Release notes。

## 0.1.1

### App

#### 新增

- 历史列表新增来源设备筛选：多选下拉可按设备过滤；本地清单查询与前端预览同步支持该过滤
- 设备展示信息（机器名、OS 版本）改为每次从系统现取，机器改名后设备列表下次读取即生效

#### 变更

- 账号偏好与登录会话拆分：自动上传档位、自动接收剪贴板、文件过滤模式、每页数量等偏好跟随历史档案存储（档案目录下的 `preferences.json`），换账号登录互不污染，未登录的本地档案也有一份独立偏好；旧配置文件里混存的偏好字段不再读取，升级后偏好恢复默认值
- 会话 token 改存系统凭据库（Windows 凭据管理器 / macOS·iOS 钥匙串 / Linux Secret Service）：`sync-config.json` 不再明文落 token
- 设备身份改为机器级的 `device.json`，所有历史档案共用一份，切换或新建档案不再继承、复制设备 id
- 移除设置页的"立即上传"回扫：调高自动上传档位后不再补传历史条目内容，相关候选查询命令一并移除
- 同步操作与 WebSocket 解耦：登录、历史对账、设备列表、条目发布与删除、文件上传下载全部改走 HTTP，WebSocket 仅用于接收服务器实时推送；WebSocket 不可用时同步功能完整可用，仅收不到实时推送（断线窗口内其他设备的变化在下次启动登录时补齐）
- 登录成功后立即通过 HTTP 拉取设备列表与对账快照：全新安装或重新登录不再要求 WebSocket 连接成功即可看到完整历史
- 复制的剪贴板内容约 2 秒内自动发布到服务器；断网期间本地捕获的记录在网络恢复后自动按捕获顺序补发，不再等待重连事件

#### 修复

- macOS 快捷粘贴：隐藏粘贴窗口后先把前台还给之前的应用再合成 `Cmd+V`，避免内容被粘贴进主界面搜索框
- 标题栏不再显示抓取手型光标

### Server

#### 新增

- `/entries/manifest` 支持按来源设备过滤（`deviceIds` 多选）
- 管理端新增设备"下线"操作：仅吊销该设备的登录会话，设备下次登录后恢复同步，设备信息与剪贴板条目保留

#### 变更

- 管理端删除设备时，该设备同步到服务器的剪贴板条目一并删除，并向该账号其余在线设备广播 `clipboard.deleted` 以同步移除本地副本；内容字节由垃圾回收统一回收

## 0.1.0

### App

#### 新增

- 提供 Windows、macOS 与 Linux 桌面客户端，并支持构建 Android APK/AAB 和 iOS unsigned IPA
- 自动采集文本、富文本、图片、文件与文件夹剪贴板，保存本地历史并支持搜索、删除和清理
- 提供快速粘贴窗口，可通过 `Ctrl/Cmd + Shift + V` 搜索历史并粘贴回原应用
- 同账号在线设备可实时接收文本、富文本和图片并直接写入系统剪贴板；文件和文件夹只同步到历史，避免自动产生无用临时文件
- 支持多文件和完整目录结构；本机粘贴优先复用原路径，远端内容仅在实际粘贴时按需下载或恢复
- 文件按内容寻址并支持秒传、分片续传和源设备中继，相同内容改名或移动后无需重复上传
- 支持登录、注册、设备管理和每台设备独立的自动接收与上传阈值；服务不可用时自动降级为纯本地模式
- Android 与 iOS 复用账号、设备、历史、搜索、删除和前台同步能力
- Android 支持从系统分享面板接收文字、图片和文件，并复用本机剪贴板采集与同步链路

#### 变更

- 移除条目固定（pin）功能：本地历史统一保留最新 200 条，"清除"按钮改为清空全部历史
- 登录响应改为通过 `settings` 字段下发服务器能力上限（单文件存储上限、单用户最大历史条数、单次复制文件数上限）
- 设置新增"同步历史每页数量"（10/20/50/100 条），连接同步服务后按该数量拉取对账快照
- 移除写死的单次复制文件数上限（原固定 1000），改为跟随服务器下发的配置

### Server

#### 新增

- 提供基于 Fastify、WebSocket 和 SQLite 的多账号同步服务，账号之间的历史、设备和文件完全隔离
- 使用加盐密码哈希和可过期设备会话，并提供登录限流与密码修改后的会话失效处理
- 按用户维护内容寻址文件池，支持秒传、分片续传、完整性校验、垃圾回收和在线源设备中继
- 提供管理后台，可配置管理员密码、HTTPS 证书、服务器文件上限和断点续传有效期
- 提供 Docker 服务端镜像，以及 Windows、macOS、Linux、Android 和 iOS 的 GitHub Release 构建流程

#### 变更

- 相同内容重复发布仅刷新条目时间（重新复制回到顶部），其余已存储数据不再被覆盖
- 移除条目固定功能及 `/entries/manifest` 的 `pinned` 查询参数，并自动迁移旧用户数据库
- 管理后台的服务器文件上限、断点续传有效期改为下拉选项，并新增"单用户最大历史条数"与"单次复制文件数上限"配置；发布新条目后超过上限的最旧记录会被自动删除并同步到各设备
- `/entries/manifest` 支持客户端指定每页数量（10-100，缺省仍为 100）
